# --- Supabase ---
# Server-only — bypasses RLS. NEVER expose to browser.
# Grab from Supabase dashboard → Project Settings → API → service_role secret.
SUPABASE_URL=https://uemilwdxnwzbgqfocbyy.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Browser-safe — RLS-gated. Use the new publishable key format.
VITE_SUPABASE_URL=https://uemilwdxnwzbgqfocbyy.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_cGIHcddpDSgOVfNGgYLSLA_B77sgSNF

# --- Client-safe (the proxy origin only) ---
VITE_API_BASE_URL=/api
VITE_APP_PASSWORD=your_app_password

# Optional: shared-secret header the /api/sync helpers require from the
# client until real session auth lands. Leave blank to disable in local dev.
PROXY_SHARED_SECRET=
