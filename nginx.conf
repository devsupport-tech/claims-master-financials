# DEPRECATED — kept for reference only.
#
# As of the cross-app sync work (server-side PAT), this app no longer ships
# nginx. The Node sidecar (server/index.ts) serves both the static SPA and
# the /api/* proxy from a single process.
#
# If you reintroduce nginx in front of the sidecar, route /api/* to the
# Node process and everything else to the static dist:
#
#   location /api/ {
#       proxy_pass http://127.0.0.1:3001;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   }
#   location / {
#       try_files $uri $uri/ /index.html;
#   }
